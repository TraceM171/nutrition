{
  "type": "module",
  "scripts": {
    "test": "node --test src/domain/tests/*.test.js",
    "build": "node build.js",
    "dev": "npx serve . --listen 8080"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
