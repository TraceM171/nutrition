- when adding ingredient, upon changing unit, do not adapt amount, default should be 1 and not change upon unit change
- no network search results in ingredient search hides local results
- add button in external ingredient to refresh its info from source
- in forked item dialog, upon clicking on original item, it should show original item dialog, not nutritiona data directly
- Revamp weekly plan tab
- undo snackbar: plan remove, extra food remove, recipe delete, custom ing delete — 5s auto-dismiss
- fix servings mess
- foods tab search: click result opens nutrition directly (skip intermediate view)
- fork icon in custom ingredients list changed to green (var(--good)).
- edit custom ingredient dialog: added Nutrition button → opens full nutrient breakdown modal.
- edit custom ingredient dialog: added Custom Serving section → add/edit/remove named serving (e.g. "1 cup = 240g").
- ingredient customization requires fork: removed rename/serving buttons from food manage modal; fork → custom ingredient editor is the only path for all customization.
- foods tab search: added × clear button inside input; clears input, hides results, resets detail state.
- add ingredient button in recipes: moved to sticky footer inside modal-body; bottom bar now Cancel/Save only.
- add ingredient dialog should not show (1 serving ≈ 345g) in the name (already on top).
- add ingredient dialog: title now clickable (dotted underline) → opens food manage modal for rename/serving/fork; unit selector refreshes on close.
- dialog stacking: openRecipeEditor pushes to stack when already open; bringToFront added to all modals missing it; Escape closes topmost-by-z; dynamic modals stop propagation; closing popped editor brings search modal back to front.
- removed "custom serving" option from unit selector (Custom unit… option + row + functions deleted); serving management delegated to ingredient dialog.
- add ingredient dialog back button wrong state: clearSelection() re-renders recipe list when in recipes tab mode instead of blanking everything.
