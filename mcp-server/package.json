{
  "name": "nourish-mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server giving an agent read/write access to a Nourish plan without needing the UI.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "ws": "^8.18.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "^2.0.0"
  }
}
